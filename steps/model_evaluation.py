from darts.metrics.metrics import (coefficient_of_variation,
                                   mse,
                                   rmse,
                                   coefficient_of_variation)